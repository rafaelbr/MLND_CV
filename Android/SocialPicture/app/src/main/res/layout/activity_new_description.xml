<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_new_description"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="br.com.geekfox.socialpicture.activities.NewDescriptionActivity">

    <ImageView
        app:srcCompat="@drawable/no_image"
        android:id="@+id/imgDesc"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:layout_below="@+id/imgDesc"
        android:layout_alignStart="@+id/imgDesc"
        android:layout_marginTop="28dp"
        android:id="@+id/editDesc"
        android:layout_alignEnd="@+id/imgDesc"
        android:text="Description here" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/save_button"
        android:layout_alignParentBottom="true"
        android:layout_alignEnd="@+id/editDesc"
        android:layout_marginBottom="27dp"
        android:id="@+id/imageButton4"
        android:background="@android:color/transparent"
        android:onClick="onSave" />
</RelativeLayout>
